import React from 'react'
import { Button } from 'antd'

export const CreateNewTask = () => {
    return (
        <Button className="w-full max-w-sm" size="large" type="primary">
            New task
        </Button>
    )
}
